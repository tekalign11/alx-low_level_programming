#include <stdio.h>

/**
 * main - Entry point
 * Description: Prints sum of multiples of 3 and 5
 * Return: Always success (0)
 */
int main(void)
{
	int t, a;

	for (t = 1, t < 342; t++)
		for 
		printf("%d", 
