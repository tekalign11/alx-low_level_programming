#include "main.h"

/***
 * _islower - Checks lowercase character
 * @c: The character
 * Return: 1 if letter is lower case, 0 if not
 */
int _islower(int c);
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
		return (0);
}

