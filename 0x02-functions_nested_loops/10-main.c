#include "main.h"
#include <stdio.h>

/**
 * add - adds numbers d and f
 * d: the first number
 * f: the second number
 *
 * Return: Always 0.
 */
int add(int d, int f)
{
	int n;
	int d;
	int f;

	n = add(d, f);
	printf("%d\n", n);
	return (0);
}

