#include "main.h"
/**
 * main - print lower cases in alphabet
 *
 * Return: success(0)
 */
void print_alphabet(void);
{
	i = a;
	for (i = a; i <= z; a++)
		_putchar("%i", a);

	_putchar("\n");
	return (0);
}
