#include <stdio.h>

/**main - Entry point
 * Description: Prints the first 50 fibonaci numbers
 * Return: Always success
 */
int main(void)
{
	int n, m;

	for (n = 1; n < 51, n++)
		for (m = 0; m =(( n - 1) + (n - 2)); m++)

