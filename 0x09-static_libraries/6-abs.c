#include "main.h"
/**
 * _abs - computes the absolute value of an int
 * @i: the intiger
 *
 * Return: Absolute value of int
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}

