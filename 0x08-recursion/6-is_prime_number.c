#include "main.h"

/**
 * is_prime_number - returns prime numbers
 * @n: The number
 */

int is_prime_number(int n)
{
	int i = 2;
	int j = 2;

	if (n = 2)
		return (0);
	else if ((n != j) && (n % j == 0))
	{
	 	is_prime_number(n);

		return (1);
	else
	{
			
