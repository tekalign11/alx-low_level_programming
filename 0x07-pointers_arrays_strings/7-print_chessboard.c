#include "main.h"

/**
 * print_chessboard - prints chess board
 *
 * @a: the array
 * @8: The column
 */
void print_chessboard(char (*a)[8])
{
	int i, j;
	int s;

	for(i = 0; i < s; i++)
		for (j = 0; j < 8; j++)
			a[s][8];
}
