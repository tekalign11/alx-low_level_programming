#include "main.h"

/**
 *  mul - multiple of two numbers
 *  @a: first number
 *  @b: second number
 *  Return: Always success
 */
int mul(int a, int b)

{
	int a;
	int b;

	for (a = i, b = j)
		printf("%d\n", mul(i, j));
	return (0);
}

