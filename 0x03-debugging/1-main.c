#include "main.h"

/**
 * main - bring infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;

	printf("Infinite loop incoming :(\n");

	/**
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("Infinitive loop avoided: \\0/\n");

	return (0);
}
