#include "main.h"

/**
 * argstostr - concatenates arguments
 * @ac: Argument count
 * @av: Argument vector
 * Return: Concatenated string from arguments
 */
char *argstostr(int ac, char **av)
{
	in t = 0;

	while (i < ac)
	{
		argv[i];
		i++;
	}
	while (t < i)

	ptr = av
	str = malloc(
