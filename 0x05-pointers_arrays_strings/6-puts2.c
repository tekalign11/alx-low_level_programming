#include "main.h"
#include <stdio.h>

/**
 * puts2 - prints every other character
 *
 * @str: The string to be returned
 *
 */
void puts2(char *str)
{
	int i, n;
	str[n];

	for (i = 0; i < n; i += 2)
	{
		printf("%d\n", i);
	}
}
