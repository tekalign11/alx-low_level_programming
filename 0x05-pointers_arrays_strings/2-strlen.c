#include "main.h"

/**
 * _strlen - returns the length of a string.
 *
 * @s: length of a string
 * Return: success
 */
int _strlen(char *s)
{
	int nurse;

	for (nurse = 0; s[nurse] != '\0' : nurse++)
		;
	return (nurse);
}
