#include <stdio.h>

/**
 * main - entry point
 * description: prints single digit base 10 nums
 * Return: 0
 */
int main(void)
{
	int n = 0;

	while (n < 10)
		putchar(n);
		n++;
	putchar('\n');
	return (0);
}
